import 'package:flutter/material.dart';

class AfiliacionScreen extends StatelessWidget {
  const AfiliacionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Afiliación'),
      ),
    );
  }
}
